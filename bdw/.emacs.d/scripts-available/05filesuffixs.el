(setq auto-mode-alist
      (append
       (list
	(cons "\\.jsp" 'sgml-mode))
       auto-mode-alist))

(setq auto-mode-alist
      (append
       (list
	(cons "\\.dryml" 'sgml-mode))
       auto-mode-alist))

(setq auto-mode-alist
      (append
       (list
	(cons "\\.cfm" 'java-mode))
       auto-mode-alist))

(setq auto-mode-alist
      (append
       (list
	(cons "\\.cs" 'java-mode))
       auto-mode-alist))

(setq auto-mode-alist
      (append
       (list
	(cons "\\.jpf" 'java-mode))
       auto-mode-alist))

(setq auto-mode-alist
      (append
       (list
	(cons "\\.ts" 'javascript-mode))
       auto-mode-alist))


(setq auto-mode-alist
      (append
       (list
	(cons "\\.jcx" 'java-mode))
       auto-mode-alist))

(setq auto-mode-alist
      (append
       (list
	(cons "\\.tld" 'sgml-mode))
       auto-mode-alist))

(setq auto-mode-alist
      (append
       (list
	(cons "\\.m80" 'perl-mode))
       auto-mode-alist))

(setq auto-mode-alist
      (append
       (list
	(cons "\\.xml" 'html-mode)
	(cons "\\.xsd" 'html-mode)
	(cons "\\.resx" 'html-mode)
	(cons "\\.rhtml" 'html-mode)
	)
       auto-mode-alist))

(setq auto-mode-alist
      (append
       (list
	(cons "\\.rake" 'ruby-mode))
       auto-mode-alist))

